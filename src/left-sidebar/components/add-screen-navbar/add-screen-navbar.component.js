import React, {
    Component
} from 'react';

import Navbar from './add-screen-navbar.component.jsx';

export const AddScreenNavbarComponent = ({
    headerProps
}) => < Navbar headerProps = {
    headerProps
}
/>